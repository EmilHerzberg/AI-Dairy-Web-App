#──────────────────────────────
# Server configuration
#──────────────────────────────
PORT=5000                     # Local port for Express

#──────────────────────────────
# MongoDB
#──────────────────────────────
# Replace the placeholders in <angle-brackets>.
# If you hit IPv6 issues on Windows you can append “&family=4”.
MONGODB_URI=mongodb+srv://<DB_USER>:<DB_PASSWORD>@cluster0.<cluster-id>.mongodb.net/<DB_NAME>?retryWrites=true&w=majority&appName=Cluster0

#──────────────────────────────
# Authentication
#──────────────────────────────
JWT_SECRET=pleaseChangeMe      # A long, random string
TOKEN_EXPIRES_IN=3600          # Seconds (3600 = 1 hour)


